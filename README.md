# HNRY tax calculator quickstart

1. Install [uv](https://docs.astral.sh/uv/getting-started/installation/)

2. `uv run tax_calculator.py` to run application

3. `uv run python -m pytest` to run tests
